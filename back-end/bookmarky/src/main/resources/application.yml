server:
  port: 8000

spring:
  application:
    name: BookMarky
    devtools:
      livereload:
        enabled: true

  profiles:
    active: db # db 정보 숨김

  jpa:
    show-sql: true
    hibernate:
      #ddl-auto: create
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true